import { css } from "@emotion/react";

export const narrationWindowStyles = {
  container: css({
    height: "4rem",
    width: "100%",
    fontSize: "0.8rem",
  }),

  text: css({
    margin: "0",
  }),
};
