import { css } from "@emotion/react";

export const enemyDisplayStyles = {
  container: css({
    maxWidth: "100px",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    margin: "0 0.5rem",
  }),

  imageContainer: css({
    marginTop: "1rem",
  }),
};
